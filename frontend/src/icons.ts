/**
 * One icon weight for the whole interface. Lucide's own default is 2, which reads heavier than
 * this product's type; every icon inherits this value from the LucideProvider in the root layout,
 * so individual icons never set strokeWidth themselves.
 */
export const ICON_STROKE_WIDTH = 1.5;
